# Завантажується лише для `npm run dev` (NODE_ENV=development).
# Канонічна адреса для sitemap / Open Graph під час розробки.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Рядок PostgreSQL задайте в .env (не в git): DATABASE_URL=postgresql://...
# Див. .env.example. Без цього dev-сервер не зможе підключитися до БД.
